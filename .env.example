# 文件传输助手 - 环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改配置

# === 应用基础配置 ===
APP_HOST=0.0.0.0
APP_PORT=8000
APP_SECRET=changeme_to_secure_password
DEBUG=true

# === 数据库配置 ===
DATABASE_URL=sqlite:///./files.db

# === 文件存储配置 ===
UPLOAD_DIR=./uploads
TEMP_DIR=./temp
MAX_FILE_SIZE=104857600  # 100MB (字节)

# === 安全配置 ===
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://localhost:5173

# === 文件去重配置 ===
HASH_ALGORITHM=sha256
ENABLE_FILE_DEDUP=true

# === 异步任务配置 ===
MAX_HASH_WORKERS=2
HASH_CHUNK_SIZE=65536  # 64KB

# === 文件类型配置 ===
ALLOWED_EXTENSIONS=jpg,jpeg,png,gif,bmp,svg,pdf,txt,doc,docx,xls,xlsx,ppt,pptx,zip,rar,7z,tar,gz,mp4,mp3,avi,mkv,mov

# === 清理配置 ===
FILE_EXPIRE_DAYS=7
AUTO_CLEANUP=true
CLEANUP_INTERVAL_HOURS=24

# === WebSocket配置 ===
WS_HEARTBEAT_INTERVAL=30
WS_MAX_CONNECTIONS=100